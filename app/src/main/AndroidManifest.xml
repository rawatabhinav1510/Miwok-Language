<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.miwokupdated">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Miwok"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Miwokupdated">
        <activity android:name=".Phrases"
            android:label="PHRASES"></activity>
        <activity android:name=".ColorsConstructor"/>
        <activity android:name=".Colors"
            android:label="COLORS"/>
        <activity android:name=".FamilyMembers"
            android:label="FAMILYMEMBERS"/>
        <activity android:name=".Numbers"
            android:label="NUMBERS"/>
        <activity android:name=".NumbersConstructor" />
        <activity android:name=".NumbersAdapter" />
        <activity android:name=".ColorsAdapter" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>